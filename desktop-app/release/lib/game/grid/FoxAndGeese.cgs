/*${cgsuite.banner}*/

class FoxAndGeese extends GridGame

    override method FoxAndGeese(grid)
        this.GridGame(grid);
    end
    
    override property CharMap.get
        return ".xo";
    end
    
    override property Icons.get
        return
        [
            GridIcon.Blank,
            GridIcon.BlackStone,
            GridIcon.WhiteStone
        ];
    end


end