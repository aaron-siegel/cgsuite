/*${cgsuite.banner}*/

/** A boolean value (`true` or `false`).
  */
system class Boolean

  external def op and(that as Boolean) as Boolean;

  external def op or(that as Boolean) as Boolean;

  external def op not as Boolean;

end
