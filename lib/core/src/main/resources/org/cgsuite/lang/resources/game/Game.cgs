/*${cgsuite.banner}*/

system class Game

  external def CanonicalForm;

  def Options(Player player) begin
    error("Subclasses of Game must override the Options method.")
  end

  def Decomposition := [this];

end
