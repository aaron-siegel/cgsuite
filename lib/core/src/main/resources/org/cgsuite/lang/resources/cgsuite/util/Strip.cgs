/*${cgsuite.banner}*/

system class Strip extends Grid

  override external static def Parse(str as String, charMap as String);

  external def Length;
  external def Substrip(first as Integer, last as Integer);
  override external def ToString(charMap as String);
  override external def Updated(newValues as Map);

end
