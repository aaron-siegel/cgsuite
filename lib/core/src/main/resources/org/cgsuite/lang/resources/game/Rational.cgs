/*${cgsuite.banner}*/

system class Rational extends SurrealNumber

  override external def ToOutput;

  override external def Birthday;
  external def Ceiling;
  external def Floor;
  external def IsDyadic;
  external def Max(that as Rational);
  external def Min(that as Rational);

end
