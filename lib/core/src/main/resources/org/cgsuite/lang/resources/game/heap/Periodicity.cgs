/*${cgsuite.banner}*/

system class Periodicity

  external def Period;
  external def Preperiod;
  external def Saltus;

  override def ToOutput :=
    "Periodicity(Period => " + Period.ToOutput + ", Preperiod => " + Preperiod.ToOutput + ", Saltus => " + Saltus.ToOutput + ")";

end
