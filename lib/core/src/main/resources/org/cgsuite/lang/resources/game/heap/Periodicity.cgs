/*${cgsuite.banner}*/

system class Periodicity

  external def Period as Integer;

  external def Preperiod as Integer;

  external def Saltus as Integer;

//  override def ToOutput :=
//    cgsuite.util.output.TextOutput("Periodicity(Period => ") + Period.ToOutput + ", Preperiod => " + Preperiod.ToOutput + ", Saltus => " + Saltus.ToOutput + ")";

end
