/*${cgsuite.banner}*/

system class Map of (`K, `V) extends Collection of MapEntry of (`K, `V)

  external def op[](key as `K) as `V;

  external def ContainsKey(key as Object) as Boolean;

  external def Keys as Set of `K;

  external def Values as Collection of `V;

end
