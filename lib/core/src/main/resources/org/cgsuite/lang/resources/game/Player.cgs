/*${cgsuite.banner}*/

system enum Player

  external Left,
  external Right;

  external def Sign as Integer; // := if this === Left then 1 else -1 end;
  external def Opponent as Player; // := if this === Left then Right else Left end;
  external def Ordinal as Integer; // := if this == Left then 1 else 2 end;

end
