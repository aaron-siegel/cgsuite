/*${cgsuite.banner}*/

system class Uptimal extends CanonicalShortGame

  external override def op+(that as Uptimal) as Uptimal;

  external override def op-(that as Uptimal) as Uptimal;

  external override def op unary+ as Uptimal;

  external override def op unary- as Uptimal;

  external def UptimalExpansion as UptimalExpansion;

end
