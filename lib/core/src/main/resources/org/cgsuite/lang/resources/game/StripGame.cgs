/*${cgsuite.banner}*/

class StripGame(strip) extends Game

  if strip is Strip then
  elseif strip is String then
    strip := Strip.Parse(strip, CharMap)
  end

  def CharMap := error("Subclasses of StripGame must override CharMap.");

end
