class Procedure : java "org.cgsuite.lang.CgsuiteProcedure"

    method Iterate(Integer n)
    
        return x ->
            for k from 1 to n do
                x := this(x);
            end

    end
    
end
