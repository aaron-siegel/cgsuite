class FoxAndGeese extends GridGame

    private static var CharMap := ".xo";
    private static var Icons := [
        GridIcon.Blank,
        GridIcon.BlackStone,
        GridIcon.WhiteStone
        ];

    public method FoxAndGeese(grid)
        this.GridGame(grid);
    end

    

end