/*
 * TestBadStaticInitializer.cgs
 *
 * Created by asiegel on Aug 15, 2011
 */

class TestBadStaticInitializer

    // Tries to assign a value to a var that doesn't exist
    static var x := (tricky := 6);

end